KAFKA_BOOTSTRAP=kafka:9092
BEHAVIOR_TOPIC=user-behavior
RULE_TOPIC=risk-rules

# multi_category | clickstream | ieee_cis
PROFILE=multi_category
RULE_VERSION=1

# Input/output files
INPUT_CSV=/workspace/data/kaggle-events.csv
RULE_FILE=/workspace/samples/generated-risk-rules.json

# -1 means all rows
MAX_ROWS=-1
